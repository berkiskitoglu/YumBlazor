@page "/Account/Manage/PersonalData"

@inject IdentityUserAccessor UserAccessor

<PageTitle>Kişisel Veriler</PageTitle>

<StatusMessage />
<h3>Kişisel Veriler</h3>

<div class="row">
    <div class="col-md-6">
        <p>Hesabınız, bize verdiğiniz kişisel verileri içerir. Bu sayfa, bu verileri indirmenize veya silmenize olanak tanır.</p>
        <p>
            <strong>Bu verileri silmek, hesabınızı kalıcı olarak kaldırır ve geri alınamaz.</strong>
        </p>
        <form action="Account/Manage/DownloadPersonalData" method="post">
            <AntiforgeryToken />
            <button class="btn btn-primary" type="submit">İndir</button>
        </form>
        <p>
            <a href="Account/Manage/DeletePersonalData" class="btn btn-danger">Sil</a>
        </p>
    </div>
</div>

@code {
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    protected override async Task OnInitializedAsync()
    {
        _ = await UserAccessor.GetRequiredUserAsync(HttpContext);
    }
}
